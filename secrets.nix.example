{
  email = "your-email@example.com";
  domain = "home.yourdomain.com";
  # External subdomains exposed on the internet (managed with DDNS)
  externalSubdomains = [];

  adminPassword = "";

  # Email alerting configuration (for Prometheus Alertmanager)
  alertEmail = "alerts@yourdomain.com";  # Email to receive alerts
  alertEmailPassword = "your-app-password";  # Fastmail app password

  # Linode API token with Domain:Read_Write permissions
  linode.apiToken = "";

  # ==============================================
  # API KEYS (auto-generated on first setup)
  # ==============================================
  # These are declaratively injected into services
  # Generate with: openssl rand -hex 32

  apiKeys = {
    sonarr = "generate_with_openssl_rand_hex_32";
    radarr = "generate_with_openssl_rand_hex_32";
    prowlarr = "generate_with_openssl_rand_hex_32";
    bazarr = "generate_with_openssl_rand_hex_32";
    sabnzbd = "generate_with_openssl_rand_hex_32";
    jellyfin = "generate_with_openssl_rand_hex_32";
    jellyseerr = "generate_with_openssl_rand_hex_32";
  };

  # ==============================================
  # USENET SERVERS
  # ==============================================
  # List of usenet providers, ordered by priority (lower = higher priority)

  usenetServers = [
    {
      host = "news.newshosting.com";
      port = 563;
      username = "your-username";
      password = "your-password";
      connections = 100;
      priority = 0;
    }
    {
      host = "news.tweaknews.eu";
      port = 563;
      username = "your-username";
      password = "your-password";
      connections = 8;
      priority = 10;
    }
  ];

  # ==============================================
  # SUBTITLE PROVIDERS
  # ==============================================

  opensubtitles = {
    username = "your-username";
    password = "your-password";
  };
}
